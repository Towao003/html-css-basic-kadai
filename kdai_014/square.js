const calculateSquare = (number) => {
    const result = number ** 2;
    console.log("計算結果:", result);
    return result;
};

const inputNumber = 10; 
const squaredResult = calculateSquare(inputNumber);
